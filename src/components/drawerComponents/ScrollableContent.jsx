const ScrollableContent = ({ children }) => (
	<div
		style={{
			background: '#2d2a30',
			color: '#fff',
			height: "100",
			padding: "5px",
		}}
	>
		{children}
	</div>
);

export default ScrollableContent;
