const ScrollableContent = ({ children }) => (
	<div
		style={{
			background: '#322f35',
			color: '#fff',
			height: "100",
			padding: "5px",
		}}
	>
		{children}
	</div>
);

export default ScrollableContent;
